#include "main.h"

/**
 * print_alphabet_x10 - Prints 10 times the alphabet, in lowercase.
 *and print new line
 * Return: Always 0
 *
 */
void print_alphabet(void)

{
	char la = lower_ alphabet;
	for(la = 'a' , la <= 'z',la++);
		_putchar(la);
	_putchar('\n');
}
